namespace Compiler {
    public enum SymbolType {
        Identifier,
        Int,
        Bool,
        Equals,
        Print,
        If,
        Then,
        End,
        Operator,
        Integer,
        Boolean,
        LParen,
        RParen,
        Minus,
        EOF
    }
}